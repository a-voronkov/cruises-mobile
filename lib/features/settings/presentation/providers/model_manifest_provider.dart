/// This feature used to define its own model manifest + download providers.
///
/// They are now shared in `core/` because both the first-run ModelSetup flow
/// and Settings -> AI Models screen need them.
export '../../../../core/providers/model_download_providers.dart';

