# CI/CD Optimization Guide

## 🚀 Overview

Оптимизированный CI/CD workflow с раздельными job'ами и интеллектуальным кешированием для максимальной производительности.

## 📊 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                         SETUP JOB                           │
│  - Install Flutter SDK                                      │
│  - Install dependencies (pub get)                           │
│  - Run code generation (build_runner)                       │
│  - Run tests & analyze                                      │
│  - Cache everything                                         │
└─────────────────┬───────────────────────────────────────────┘
                  │
         ┌────────┴────────┐
         ▼                 ▼
┌─────────────────┐ ┌─────────────────┐
│ BUILD ANDROID   │ │   BUILD iOS     │
│ - Restore cache │ │ - Restore cache │
│ - Build llama   │ │ - Build llama   │
│ - Build APK/AAB │ │ - Build IPA     │
└────────┬────────┘ └────────┬────────┘
         │                   │
         └────────┬──────────┘
                  ▼
         ┌─────────────────┐
         │ CREATE RELEASE  │
         │ - Download all  │
         │ - Create GitHub │
         │   release       │
         └─────────────────┘
```

## 🎯 Ключевые особенности

### 1. Раздельные Job'ы

**Setup Job:**
- Устанавливает все зависимости один раз
- Запускает тесты и анализ кода
- Создаёт кеши для последующих job'ов
- Передаёт generated код через артефакты

**Build Jobs (параллельно):**
- Восстанавливают кеши из setup
- Компилируют llama.cpp (с кешированием)
- Собирают приложения
- Загружают артефакты

**Release Job:**
- Скачивает все артефакты
- Создаёт GitHub release
- Прикрепляет файлы к релизу

### 2. Интеллектуальное кеширование

| Кеш | Ключ | Размер | Экономия времени |
|-----|------|--------|------------------|
| **Flutter SDK** | `flutter-{os}-{version}` | ~500 MB | ~2 мин |
| **Pub dependencies** | `pub-{os}-{hash(pubspec.lock)}` | ~200 MB | ~1 мин |
| **Generated code** | `generated-{os}-{hash(pubspec+dart)}` | ~10 MB | ~30 сек |
| **Gradle** | `gradle-{os}-{hash(gradle files)}` | ~300 MB | ~1 мин |
| **CocoaPods** | `pods-{os}-{hash(Podfile.lock)}` | ~400 MB | ~2 мин |
| **llama.cpp Android** | `llama-android-{hash(version.txt)}` | ~50 MB | ~7 мин |
| **llama.cpp iOS** | `llama-ios-{hash(version.txt)}` | ~100 MB | ~10 мин |

**Общая экономия:** ~20-25 минут на каждой сборке! 🎉

### 3. Умные Restore Keys

Каждый кеш имеет fallback restore keys для частичного восстановления:

```yaml
key: pub-macos-abc123def456
restore-keys: |
  pub-macos-abc123  # Точное совпадение версии
  pub-macos-        # Любая версия для этой ОС
```

Это означает, что даже если `pubspec.lock` изменился, мы восстановим большую часть зависимостей.

## 📈 Производительность

### Первая сборка (без кешей)

```
Setup:           ~5 мин
  ├─ Flutter:    ~2 мин
  ├─ Pub get:    ~1 мин
  ├─ Codegen:    ~30 сек
  ├─ Tests:      ~1 мин
  └─ Analyze:    ~30 сек

Build Android:   ~10 мин
  ├─ Gradle:     ~1 мин
  ├─ llama.cpp:  ~7 мин
  └─ Flutter:    ~2 мин

Build iOS:       ~15 мин
  ├─ CocoaPods:  ~2 мин
  ├─ llama.cpp:  ~10 мин
  └─ Flutter:    ~3 мин

Release:         ~1 мин

TOTAL:           ~31 мин
```

### Последующие сборки (с кешами)

```
Setup:           ~2 мин
  ├─ Restore:    ~30 сек
  ├─ Tests:      ~1 мин
  └─ Analyze:    ~30 сек

Build Android:   ~3 мин
  ├─ Restore:    ~30 сек
  └─ Flutter:    ~2.5 мин

Build iOS:       ~4 мин
  ├─ Restore:    ~30 сек
  ├─ Pods:       ~30 сек
  └─ Flutter:    ~3 мин

Release:         ~1 мин

TOTAL:           ~10 мин
```

**Экономия: ~68% времени!** ⚡

### После обновления llama.cpp

```
Setup:           ~2 мин (кеш)
Build Android:   ~10 мин (пересборка llama)
Build iOS:       ~15 мин (пересборка llama)
Release:         ~1 мин

TOTAL:           ~28 мин
```

Следующая сборка: ~10 мин (новый кеш llama.cpp)

## 🔧 Инвалидация кешей

### Автоматическая инвалидация

Кеши автоматически инвалидируются при изменении:

1. **Flutter SDK**: Обновление `FLUTTER_VERSION` в workflow
2. **Pub dependencies**: Изменение `pubspec.lock`
3. **Generated code**: Изменение `pubspec.lock` или `.dart` файлов
4. **Gradle**: Изменение `.gradle` файлов
5. **CocoaPods**: Изменение `Podfile.lock`
6. **llama.cpp**: Изменение `scripts/llama-version.txt`

### Ручная инвалидация

Для принудительной пересборки:

```bash
# Обновить версию llama.cpp
echo "новый_commit_hash" > scripts/llama-version.txt
git commit -am "Force rebuild llama.cpp"
git push

# Обновить Flutter версию
# Отредактируйте .github/workflows/build.yml:
env:
  FLUTTER_VERSION: '3.20.0'  # новая версия
```

## 📦 Артефакты

### Между Job'ами

**Setup → Build:**
- `generated-code` (retention: 1 день)
  - `**/*.g.dart`
  - `**/*.freezed.dart`
  - `**/*.config.dart`

### Финальные артефакты

**Build → Release:**
- `android-apk` (retention: 30 дней)
- `android-aab` (retention: 30 дней)
- `ios-build` (retention: 30 дней)

## 🎮 Использование

### Ручной запуск (единственный способ)

Workflow запускается **только вручную** через GitHub Actions UI или CLI.

**Через GitHub UI:**
1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите workflow **Build and Release**
4. Нажмите **Run workflow** (справа)
5. Выберите branch (main, develop, или feature branch)
6. Нажмите зелёную кнопку **Run workflow**

**Через GitHub CLI:**
```bash
# Запуск на main branch
gh workflow run build.yml --ref main

# Запуск на develop branch
gh workflow run build.yml --ref develop

# Запуск на feature branch
gh workflow run build.yml --ref feature/my-feature
```

**Через REST API:**
```bash
curl -X POST \
  -H "Accept: application/vnd.github+json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  https://api.github.com/repos/OWNER/REPO/actions/workflows/build.yml/dispatches \
  -d '{"ref":"main"}'
```

### Почему только ручной запуск?

- ✅ **Контроль**: Вы решаете, когда собирать релиз
- ✅ **Экономия ресурсов**: Не тратим время runner'а на каждый коммит
- ✅ **Гибкость**: Можно собрать любую ветку в любое время
- ✅ **Безопасность**: Нет случайных релизов при экспериментах

## 🔍 Мониторинг

### Проверка кешей

```bash
# Посмотреть логи setup job
gh run view --log | grep "Cache"

# Пример вывода:
# ✅ Cache restored from key: flutter-macos-3.19.0
# ✅ Cache restored from key: pub-macos-abc123
# ❌ Cache not found for key: llama-android-xyz789
```

### Размеры артефактов

В конце каждой сборки выводится summary:

```
## 🎉 Build Complete!

### 📦 Artifacts
- Android APK: 45.2 MB
- Android AAB: 42.8 MB

### 🏷️ Release
Tag: v123
```

## 🐛 Troubleshooting

### Кеш не восстанавливается

**Проблема**: `Cache not found`

**Решение**:
1. Проверьте, что ключ кеша правильный
2. Убедитесь, что кеш был создан в предыдущей сборке
3. Проверьте restore-keys для fallback

### Build job падает с "fail-on-cache-miss"

**Проблема**: Setup job не создал кеш

**Решение**:
1. Проверьте логи setup job
2. Убедитесь, что setup job завершился успешно
3. Временно уберите `fail-on-cache-miss: true` для отладки

### llama.cpp не компилируется

**Проблема**: Ошибка в `build-llama-*.sh`

**Решение**:
1. Проверьте, что NDK установлен (Android)
2. Проверьте, что Xcode установлен (iOS)
3. Проверьте версию в `llama-version.txt`
4. Запустите скрипт локально для отладки

### Артефакты не передаются между jobs

**Проблема**: Download artifact fails

**Решение**:
1. Проверьте, что upload успешен в предыдущем job
2. Убедитесь, что имена артефактов совпадают
3. Проверьте retention period

## 📚 Дополнительные ресурсы

- [GitHub Actions Caching](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows)
- [Flutter CI/CD Best Practices](https://docs.flutter.dev/deployment/cd)
- [CI_CD_LLAMA_SETUP.md](CI_CD_LLAMA_SETUP.md) - llama.cpp интеграция
- [scripts/README.md](../scripts/README.md) - Build scripts

---

**Дата**: 2026-01-10  
**Версия**: 2.0 (Optimized)  
**Статус**: ✅ Production Ready

